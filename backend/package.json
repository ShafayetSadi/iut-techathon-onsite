{
  "name": "vantage-dry-run-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend services for the Dry Run arm suite — IK solver and agentic/voice layer (later phases). Zero runtime deps for now.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
